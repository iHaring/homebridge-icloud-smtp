{
  "pluginAlias": "ICloudSMTP",
  "pluginType": "platform",
  "singular": false,
  "headerDisplay": "Send emails via iCloud SMTP when switches are triggered",
  "schema": {
    "type": "object",
    "required": [
      "username",
      "password"
    ],
    "properties": {
      "username": {
        "title": "iCloud email address (sender)",
        "type": "string",
        "required": true,
        "format": "email",
        "description": "Use the same email address used to log in to iCloud.com when generating the app-specific password."
      },
      "password": {
        "title": "App-specific password",
        "type": "string",
        "description": "Generate an app-specific password on https://account.apple.com."
      },
      "switches": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "object",
          "required": [
            "name",
            "subject"
          ],
          "properties": {
            "name": {
              "type": "string",
              "title": "Switch name"
            },
            "to": {
              "type": "string",
              "format": "email",
              "title": "Recipient email (optional)",
              "description": "Email address that will receive the message. If no value is provided, the email will be sent to the same address used in the username field."
            },
            "subject": {
              "type": "string",
              "title": "Email subject"
            },
            "body": {
              "type": "string",
              "title": "Email body"
            },
            "cooldown": {
              "type": "number",
              "default": 30,
              "title": "Cooldown (seconds)",
              "description": "Minimum time between trigger events for this switch."
            }
          }
        }
      }
    }
  }
}
