{
  "pluginAlias": "ICloudSMTP",
  "pluginType": "platform",
  "singular": false,
  "displayName": "iCloud SMTP",
  "headerDisplay": "Send emails via iCloud SMTP when switches are triggered",
  "schema": {
    "type": "object",
    "required": [
      "name",
      "username",
      "password"
    ],
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "iCloud SMTP",
        "description": "Name of the platform instance shown in Homebridge."
      },
      "username": {
        "title": "iCloud email address (sender)",
        "type": "string",
        "description": "Use the same email address used to log in to iCloud.com when generating the app-specific password."
      },
      "password": {
        "title": "App-specific password",
        "type": "string",
        "description": "Generate an app-specific password on https://account.apple.com."
      },
      "switches": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "object",
          "required": [
            "name",
            "subject"
          ],
          "properties": {
            "name": {
              "type": "string",
              "title": "Switch name"
            },
            "to": {
              "type": "string",
              "title": "Recipient email (optional)",
              "description": "If empty, email is sent to the sender address (username). Multiple recipients supported via comma-separated emails.",
              "pattern": "^\\s*[^,\\s@]+@[^,\\s@]+\\.[^,\\s@]+(\\s*,\\s*[^,\\s@]+@[^,\\s@]+\\.[^,\\s@]+)*\\s*$"
            },
            "subject": {
              "type": "string",
              "title": "Email subject"
            },
            "body": {
              "type": "string",
              "title": "Email body"
            },
            "cooldown": {
              "type": "number",
              "default": 30,
              "title": "Cooldown (seconds)"
            }
          }
        }
      }
    }
  }
}
